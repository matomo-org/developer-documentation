{% from "macros.twig" import class_name %}

{% for class in classes if not class.interface %}
{% if class.projectclass and 'PiwikTracker' != class.shortname and 'MatomoTracker' != class.shortname  %}
## {{ (class)|replace({'Piwik\\': ''}) }}
{% endif %}
{% endfor %}