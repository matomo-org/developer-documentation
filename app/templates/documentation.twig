{% extends 'layout/base.twig' %}

{% block content %}
    <div class="col-md-3">
        <ul class="nav nav-pills nav-stacked">
            {% for url,item in menu %}
                <li><a href="/{{ url }}"><i class="icon-chevron-right"></i> {{ item.title }}</a></li>
            {% endfor %}
        </ul>
    </div>
    <div class="col-md-9">
        {{ doc|raw }}
    </div>
{% endblock %}
